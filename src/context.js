import React from "react";

export const HandleSquareClick = React.createContext(null);
export const HandleSetStep = React.createContext(null);
export const HandleSetNextPlayer = React.createContext(null);
